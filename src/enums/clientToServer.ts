export enum ClientToServer{
    'register player' = 'register player',
    'disconnect' = 'disconnect',
    'que for game' = 'que for game',
    'cancel que for game' = 'cancel que for game',
    'decline game' = 'decline game',
    'accept game' = 'accept game'
}